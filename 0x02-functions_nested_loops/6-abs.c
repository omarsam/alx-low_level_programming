#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @int: value of testing
 *
 * Return: Always 0
 *
 * Created by Omar El-Shazly
 */

int _abs(int)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}

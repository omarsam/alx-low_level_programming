#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: value of testing
 *
 * Return: Always 0
 *
 * Created by Omar El-Shazly
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
